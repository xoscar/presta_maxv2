web:
  build: .
  ports:
    - '3000:3000'
    - '4000:4000'
  working_dir: /usr/src/app
  volumes:
    - ./:/usr/src/app
  links:
    - mongodb

mongodb:
  image: mongo
  volumes:
    - ./tools/backup:/usr/dump
  ports:
    - '27017:27017'